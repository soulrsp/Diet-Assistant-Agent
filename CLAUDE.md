# Diet Assistant Agent — 프로젝트 컨텍스트

개인용(사용자 2인: 본인 + 여자친구, 각자 폰) 다이어트 기록 앱입니다. 무료로 개발/운영하는 것을 전제로 합니다.

## 협업 지침 (항상 지켜야 할 규칙)

- **작업을 시작하기 전에, 사용자의 지시 내용을 95% 이해했다고 판단될 때까지 질문해야 합니다.** 애매한 요구사항, 결정되지 않은 기술적 선택지, 여러 해석이 가능한 지시는 임의로 추측해서 진행하지 않고 먼저 확인합니다.
- **질문 리스트와 이 CLAUDE.md 문서는 항상 존댓말로 작성합니다.**
- **Expo 관련 코드를 작성하기 전에는 [AGENTS.md](AGENTS.md)에 안내된 버전별 공식 문서를 확인합니다.** (Expo tabs 템플릿이 자동 생성한 규칙)

## 프로젝트 개요

개인용(사용자 2인: 본인 + 여자친구, 각자 폰) 다이어트 기록 앱입니다. 무료로 개발/운영하는 것을 전제로 합니다.

## 확정된 설계 결정 (Q&A 로그)

아래는 설계 단계에서 사용자와 합의한 내용입니다. 구현 시 이 결정들을 우선 따르고, 바뀔 경우 이 문서를 갱신합니다.

| # | 질문 | 결정 | 비고 |
|---|------|------|------|
| 1 | 개발 스택 | **React Native (Expo)** | 사용자가 미정 → 추천 받음. iOS/Android 동시 대응, 사이드로드 용이 |
| 2 | 배포 방식 | **사이드로드 (개인 배포)** | 앱스토어/플레이스토어 정식 등록 안 함. APK 직접 설치 / iOS는 개발자 계정으로 개인 설치 |
| 3 | 음식 인식 방식 | **자동 인식(Vision AI) 우선 + 실패 시 수동 검색** | 1차: 이미지 인식으로 음식명 추정. 정확도 문제 시 2차로 수동 검색 기능 추가 구현 |
| 4 | 데이터 저장소 | **Firebase (클라우드)** — 단, 눈바디 사진은 예외 | 식단/몸무게/설정 등은 Firebase, 나중에 두 사람 동기화 붙이기 용이 |
| 5 | 눈바디 사진 저장 위치 | **사진만 기기 내 로컬 저장**, 나머지 데이터만 클라우드 | 가장 민감한 개인 신체 이미지이므로 외부 업로드 안 함 |
| 6 | API 키 보안 | **클라이언트에 키 포함 (간단한 방식)** | 개인 사이드로드 앱이라 위험 낮음. 무료 티어 호출량 제한을 걸어 최악의 경우를 대비 |
| 7 | 영양 데이터 소스 | **1차: 범용 Vision AI로 음식명 인식 → 2차: 식약처 식품영양성분DB(공공데이터포털, 무료)로 칼로리/탄단지 조회.** 정확도 낮으면 음식 특화 AI(LogMeal 등) 추가 검토 | 한식 위주 정확도를 위해 국내 공공 데이터 활용 |
| 8 | 목표 설정 기능 | **필요함** — 목표 체중 + 일일 칼로리 목표 설정, 그래프에 목표선 표시 | |
| 9 | 벤치마킹 방식 | **Claude가 웹 조사 후 비교표 작성** (inout 외 Noom, MyFitnessPal 등) | 완료 — 설계 플랜 문서 참고 |
| 10 | 알림/리마인더 | **필요함** — 매일 지정 시간에 기록 누락 방지용 푸시 알림 | |
| 11 | 캐릭터 요소 | **넣는다** — inout처럼 포인트/미션 시스템은 넣지 않되, 지방 닮은 캐릭터가 몸무게 변화·기록 여부에 따라 표정/반응/배경이 바뀌는 정도로 가볍게 구현 | 사용자가 참고 이미지 링크 공유 (지방이 캐릭터류) |
| 12 | 식단 탭 항목 순서 | **설정 화면에서 사용자가 직접 순서 커스터마이징 가능** (아침/간식/영양제/점심/저녁 등) | 고정 순서 강제하지 않음 |
| 13 | Firebase SDK 방식 | **Firebase JS SDK (v9 모듈러)** | React Native Firebase(네이티브 모듈)는 Expo 관리형 워크플로우에 EAS 빌드가 추가로 필요해 복잡함. JS SDK는 Expo Go에서 바로 동작 |
| 14 | 사용할 Firebase 제품 | **Authentication, Firestore, Storage** | Cloud Functions는 API 키를 클라이언트에 두기로 했으므로 불필요. 알림은 Firebase 없이 `expo-notifications` 로컬 스케줄링으로 처리 |
| 15 | 인증 방식 | **이메일/비밀번호 계정 2개** (본인 + 여자친구) | 익명 로그인 대신 채택 — 추후 커플 동기화 시 계정 기준으로 연결하기 쉬움 |
| 16 | Vision AI 공급자 | **Google Cloud Vision API** | 무료 티어(월 1,000건) 제공, 이미지 라벨링에 특화 |
| 17 | 구현 순서 전략 | **화면/로직을 목업(더미 데이터·목업 함수)으로 먼저 완성 → 실제 연동은 이후 교체** | 그래프·목표·음식 인식 모두 이 방식 적용. Firebase 프로젝트 생성은 사용자가 콘솔에서 직접 진행(계정 생성은 Claude가 대신 할 수 없음) |
| 18 | Firebase 프로젝트 | **기존 프로젝트(`llm-debate-agent-36ab2`)를 재사용, 콘솔 표시 이름만 Diet-Assistant-Agent로 변경** | 새 프로젝트 대신 기존 것을 그대로 쓰기로 결정. projectId 자체는 변경 불가 |
| 19 | 로그인 유지 방식 | **한 번 로그인하면 기기에 세션 유지** (`getReactNativePersistence` + AsyncStorage) | 앱 재실행마다 로그인 요구하지 않음 |
| 20 | 추이 탭 데이터 소스 | **Firestore에 실제 몸무게 기록이 있으면 실제 데이터, 없으면 더미 데이터로 자동 전환** | `lib/trend.ts`의 `getTrendPoints()`가 처리 |
| 21 | 식단 사진 업로드 시점 | **일단 로컬 uri만 유지, Firebase Storage 업로드는 다음 단계로 보류** | 지금은 Auth+Firestore(텍스트 데이터)만 연동 완료 |

## 반영하지 않기로 한 것 (기존 앱 대비)

- **포인트/미션 시스템 없음** — 사용자가 inout에서 귀찮다고 느낀 부분입니다. 이 앱에는 도입하지 않습니다.

## 미해결 / 추후 결정 필요 (남은 리스크)

- 두 사람 데이터 동기화(커플 연동)의 구체적 방식과 시점 — "나중에 구현할 수도 있음" 상태로 보류 중입니다.
- 캐릭터 일러스트/애니메이션 리소스를 직접 그릴지, 외주/에셋 구매할지 정해지지 않았습니다.
- 앱 이름이 아직 미정입니다.
- Firestore 보안 규칙이 아직 테스트 모드(30일 후 자동 잠김, 그 전엔 인증 여부와 무관하게 개방)입니다. 실사용 전에 `request.auth.uid == userId` 조건으로 규칙을 좁혀야 합니다.
- 식단 사진의 Firebase Storage 업로드는 아직 연동 전입니다(로컬 uri만 사용 중). 다음 단계로 예정.
- Firebase 콘솔에서 이메일/비밀번호 계정 2개(본인 + 여자친구)가 실제로 생성되었는지는 Claude가 확인할 수 없습니다(로그인 필요). 사용자가 직접 로그인 테스트로 확인 필요.

## 참고 자료

- 벤치마킹 대상: inout(국내 1위 다이어트 앱), Noom, MyFitnessPal
- 영양 데이터: 식품안전나라(식약처) 식품영양성분DB — https://various.foodsafetykorea.go.kr/nutrient/, 공공데이터포털 https://www.data.go.kr
- 캐릭터 디자인 참고: 사용자가 공유한 네이버 이미지 검색 링크("지방이 캐릭터")
